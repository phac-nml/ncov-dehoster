// Nanopore Parameters
params {

    // Nanopore fast5 reads directory
    directory = false

    // Minimum fastq read length
    min_length = 1600

    // Maximum fastq read length
    max_length = 2400
}